#!/bin/sh 

# script to toggle compton compositor

pgrep -x picom > /dev/null &&  # check if compton is running
	pkill picom || # if running kill it
	picom -b --config ~/.config/picom/picom.conf # else start compton

exit 0
